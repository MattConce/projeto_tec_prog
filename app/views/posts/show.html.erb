<% provide(:title, 'Depoimentos') %>
<p id="notice"><%= notice %></p>

<p>
  <b>Name:</b>
  <%= @user.name %>
</p>

<p>
  <b>Title:</b>
  <%= @post.title %>
</p>

<h2>Criar depoimento</h2>
<%= form_for([@user, @user.posts.build]) do |f| %>
  <div class="field">
    <%= f.label "Título" %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label "Depoimento" %><br />
    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= link_to 'Voltar', posts_path %>
