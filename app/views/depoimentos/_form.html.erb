<%= form_with(model: depoimento, local: true) do |form| %>
  <% if depoimento.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(depoimento.errors.count, "error") %> prohibited this depoimento from being saved:</h2>

      <ul>
      <% depoimento.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :texto %>
    <%= form.text_area :texto, id: :depoimento_texto %>
  </div>

  <div class="field">
    <%= form.label :usuario_id %>
    <%= form.number_field :usuario_id, id: :depoimento_usuario_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
